<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Otro Mundo | Colección</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&family=Quicksand:wght@400;700&family=Pacifico&family=Montserrat:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #25d366;
            --accent: #ffbb3b;
            --bg-light: #f8fff8;
            --shadow: 0 2px 16px rgba(37, 211, 102, 0.13);
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Quicksand', 'Inter', sans-serif;
            color: #222; background: linear-gradient(135deg, var(--bg-light) 0%, #e2ffe3 100%);
        }
        header {
            position: fixed; top: 0; width: 100%; backdrop-filter: blur(14px);
            background: rgba(255, 255, 255, 0.9); box-shadow: var(--shadow);
            padding: 15px 0;
        }
        .container { max-width: 1200px; margin: auto; padding: 0 20px; }
        nav { display: flex; justify-content: space-between; align-items: center; }
        .logo { font-size: 1.75rem; font-family: 'Pacifico', cursive; color: var(--primary); }
        .nav-links { display: flex; list-style: none; gap: 1.5rem; }
        .nav-links a { font-weight: 700; color: var(--primary); text-decoration: none; transition: color .3s; }
        .nav-links a:hover { color: var(--accent); }
        
        /* HERO */
        .hero {
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            min-height: 100vh;
            background: url('img/hero-bg.jpg') center/cover fixed;
            color: #fff;
            position: relative;
        }
        .hero::before {
            content: "";
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: rgba(0, 0, 0, 0.4);
        }
        .hero-content {
            position: relative;
            max-width: 600px;
            animation: fadeInUp 1s ease-out;
        }
        .hero-title {
            font-size: 3.5rem;
            font-weight: bold;
            color: #90EE90;
            text-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(45deg, var(--primary), var(--accent));
            color: #fff;
            padding: 15px 30px;
            font-size: 1.2rem;
            border-radius: 30px;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .cta-button:hover {
            transform: scale(1.07);
            box-shadow: 0 8px 16px rgba(255, 187, 59, 0.5);
        }

        /* SECCIÓN COLECCIÓN */
        .products-section {
            padding: 80px 0;
            background: white;
        }
        .products-grid {
            display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem;
        }
        .product-card {
            background: white