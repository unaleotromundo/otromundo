// src/components/plant-card.tsx"use client";import Image from "next/image";import type { Plant } from "@/lib/plant-types";import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";import { ScrollArea } from "@/components/ui/scroll-area";interface PlantCardProps {  plant: Plant;}export function PlantCard({ plant }: PlantCardProps) {  return (    <Card className="w-full max-w-sm overflow-hidden shadow-lg hover:shadow-xl transition-shadow duration-300">      <CardHeader className="p-0">        {plant.photoDataUri && (          <div className="relative w-full h-64">            <Image              src={plant.photoDataUri}              alt={plant.name || "Imagen de la planta"}              layout="fill"              objectFit="cover"              data-ai-hint="plant nature"            />          </div>        )}      </CardHeader>      <CardContent className="p-4">        <CardTitle className="text-xl font-semibold mb-2 text-primary">{plant.name || "Planta sin Nombre"}</CardTitle>        <ScrollArea className="h-24">          <CardDescription className="text-sm text-foreground/80">            {plant.description || "No hay descripción disponible."}          </CardDescription>        </ScrollArea>      </CardContent>    </Card>  );}