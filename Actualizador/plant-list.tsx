// src/components/plant-list.tsx"use client";import type { Plant } from "@/lib/plant-types";import { PlantCard } from "./plant-card";interface PlantListProps {  plants: Plant[];}export function PlantList({ plants }: PlantListProps) {  if (plants.length === 0) {    return (      <div className="text-center py-10">        <p className="text-lg text-muted-foreground">¡Tu jardín está vacío!</p>        <p className="text-sm text-muted-foreground">Añade algunas plantas para empezar.</p>      </div>    );  }  return (    <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 p-4">      {plants.map((plant) => (        <PlantCard key={plant.id} plant={plant} />      ))}    </div>  );}